# Development profile
# Use Dockerfile-compatible environment variable names with local defaults.
spring.datasource.url=jdbc:postgresql://${DATABASE_URL:dpg-d8ptbqj7uimc73ahc5vg-a.virginia-postgres.render.com}:${DATABASE_PORT:5432}/${DATABASE_NAME:bluepatitas}
spring.datasource.username=${DATABASE_USER:root}
spring.datasource.password=${DATABASE_PASSWORD:oaaqKx6NZUdPAWV9RAI4nqihHzgHNTQN}

# Developer-friendly JPA behavior
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
# App port
server.port=${PORT:8080}